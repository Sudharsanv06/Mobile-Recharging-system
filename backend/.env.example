# ================================
# SERVER CONFIGURATION
# ================================
NODE_ENV=production
PORT=5000

# ================================
# DATABASE
# ================================
# MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
# Make sure to whitelist your deployment IP or use 0.0.0.0/0 in Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/mobile-recharge?retryWrites=true&w=majority

# ================================
# AUTHENTICATION
# ================================
# JWT secret key - MUST be at least 32 characters for production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters

# ================================
# PAYMENT GATEWAY (RAZORPAY)
# ================================
# Get these from: https://dashboard.razorpay.com/app/keys
# Use test keys (rzp_test_) for development, live keys (rzp_live_) for production
RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Webhook secret for signature verification
# Get from: https://dashboard.razorpay.com/app/webhooks
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret

# ================================
# SMS SERVICE (TWILIO)
# ================================
# Get from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# ================================
# EMAIL SERVICE (NODEMAILER)
# ================================
# For Gmail, use App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
EMAIL_FROM=noreply@yourdomain.com

# ================================
# CORS CONFIGURATION
# ================================
# Comma-separated list of allowed origins (no spaces)
# Supports wildcards (*) for Vercel preview URLs
# Include: production, www, preview branches, and development URLs
# Examples:
#   - Exact match: https://myapp.vercel.app
#   - Wildcard: https://*.vercel.app (matches all subdomains)
#   - Git branches: https://myapp-git-*.vercel.app
# For Vercel: include both production and preview patterns
ALLOWED_ORIGINS=https://your-app.vercel.app,https://*.vercel.app,http://localhost:5173

# ================================
# ERROR TRACKING (OPTIONAL)
# ================================
# Get from: https://sentry.io/
SENTRY_DSN=https://xxxxxxxx@sentry.io/xxxxxxx

# ================================
# REDIS (OPTIONAL)
# ================================
# For rate limiting and caching
# Format: redis://username:password@host:port
REDIS_URL=redis://username:password@host:port

# ================================
# SOCKET.IO (OPTIONAL)
# ================================
# CORS origin for Socket.io connections
SOCKET_CORS_ORIGIN=https://your-frontend-domain.com
