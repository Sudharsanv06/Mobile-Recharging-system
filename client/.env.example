# Example environment variables for the client (Vite)
# Set the backend API base URL (no trailing slash)
VITE_API_BASE=http://localhost:5000
# Optional: supply a Razorpay test key id to use in client-side logging or debug flows.
# The server will normally return the correct `keyId` when creating an order.
VITE_RAZORPAY_KEY_ID=rzp_test_yourKeyHere
